{
  "name": "mage2",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace @mage2/schema && npm run build --workspace @mage2/player && npm run build --workspace @mage2/player-ui && npm run build --workspace @mage2/media",
    "build:apps": "npm run build --workspace @mage2/runtime-web && npm run build --workspace @mage2/editor",
    "build": "node scripts/build-workspace.mjs",
    "dev:editor": "npm run build:packages && npm run dev --workspace @mage2/editor",
    "dev:runtime": "npm run build:packages && npm run dev --workspace @mage2/runtime-web",
    "package:editor:win": "npm run build:packages && npm run build --workspace @mage2/runtime-web && npm run build --workspace @mage2/editor && node scripts/package-editor-win.mjs",
    "package:runtime:win": "node scripts/package-runtime-win.mjs",
    "check:sensitive-paths": "node scripts/check-sensitive-paths.mjs",
    "test:sensitive-paths": "node --test scripts/check-sensitive-paths.node-test.mjs",
    "pretest": "npm run check:sensitive-paths",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspace @mage2/player-ui && npm run typecheck --workspace @mage2/editor && npm run typecheck --workspace @mage2/runtime-web",
    "verify:player:parity": "node scripts/verify-shared-player-parity.mjs",
    "verify:editor:windows-launch": "node scripts/verify-editor-windows-launch.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vitejs/plugin-react": "^5.0.4",
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0",
    "electron-builder": "^26.0.12",
    "playwright": "^1.62.1",
    "rimraf": "^6.1.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3",
    "vite": "^7.1.7",
    "vitest": "^3.2.4",
    "wait-on": "^8.0.5"
  }
}
